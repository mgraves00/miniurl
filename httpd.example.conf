server "default" {
	listen on 127.0.0.1 port 5000
	root "/miniurl"
	location "/static/*" {
		directory { no index }
	}
	location "/webfonts/*" {
		directory { no index }
	}
	location match "/.*" {
		fastcgi {
			socket "/slowcgi.sock"
			param SCRIPT_FILENAME "/miniurl/url"
		}
	}
}

